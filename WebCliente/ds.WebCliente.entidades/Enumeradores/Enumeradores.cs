using System;
using System.Collections.Generic;
using System.Text;

namespace bd.log.entidades.Enumeradores
{
    /*
     Creado por DigitalStrategy
     Los enumeradores normalmente suele ser recomendable definir una enumeración directamente dentro de un espacio de nombres 
     para que todas las clases de dicho espacio puedan tener acceso a esta con la misma facilidad. Sin embargo, 
     una enumeración también puede anidarse dentro de una clase o estructura. 
 */
    public enum Aplicacion
    {
       DsWebCliente
    }
}
